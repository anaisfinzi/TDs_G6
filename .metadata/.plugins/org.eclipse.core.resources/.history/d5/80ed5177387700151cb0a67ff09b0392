/**
 * 
 */
package td6_7.exo3_2;

/**
 * @author fcamps
 *
 */

class ProdForN extends Thread 
{
	FramedBufferN buf;
	int id;

	public ProdForN(FramedBufferN b, int _id) 
	{
		buf = b;
		id = _id;
	}

	public void run() 
	{
		while (true)
		{
			buf.deposer(new Integer(1), id);
			yield();
		}
	}
}

/**
 * 
 * @author fcamps
 *
 */
class ConsForN extends Thread 
{
	FramedBufferN buf;
	int id;

	public ConsForN(FramedBufferN b, int _id) 
	{
		buf = b;
		id = _id;
	}

	public void run()
	{
		while (true)
		{
			buf.prelever(id);
			yield();
		}
	}
}

/**
 * 
 * @author fcamps
 *
 */
public class TestFramedNBuffer {

	/**
	 * @param args
	 */
	public static void main(String[] args) {

		FramedBufferN buff = new FramedBufferN(20);

		new ProdForN(buff, 1).start();
		new ProdForN(buff, 2).start();
		new ProdForN(buff, 3).start();
		new ConsForN(buff, 1).start();
		new ConsForN(buff, 2).start();
		new ConsForN(buff, 3).start();

	}

}


